# Event URL is the only discovery mechanism

Unlike typical “my Events” apps, there is no Event list. The unguessable URL is the capability: anyone signed in who has it can view and mark Availability; losing the URL means the Organizer cannot find the Event again. We chose this over a directory so sharing stays a pasteable Discord link and the product stays a when2meet clone. Bookmarks and shared links make this expensive to reverse.
