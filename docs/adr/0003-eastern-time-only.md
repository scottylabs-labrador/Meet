# Every Event is America/New_York

The audience is CMU, so Event dates and daily hours are stored as calendar dates plus minutes from midnight Eastern — not UTC instants and not per-User timezones. Adding conversion later would be a data migration, not a display tweak. The alternative (per-User or per-Event timezone) was rejected for v1 to keep the grid one shared campus clock.
