[project]
name = "db"
revision = "1.0"
extends = ["../../secretspec.toml"]

[profiles.default]
DATABASE_URL = { description = "Database connection string", required = true }

[profiles.local]
DATABASE_URL = { default = "postgresql://postgres:donotuseinprod@localhost:5432/meet", ref = { item = "meet/local/server", field = "DATABASE_URL" } }
